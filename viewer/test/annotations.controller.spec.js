'use strict';

describe('AnnotationsController', function() {
    var $controller;

    beforeEach(function() {
        angular.mock.module('chaise.viewer');
        inject(function(_controller) {
            $controller = _controller;
        });
    });

    it('should have a lot of tests', function() {
        
    });
});
