// A value to hold the rows of each vocabulary table (e.g. tissue, age stage,
//embedding medium) inside one large "vocabs" object
(function() {
    'use strict';

    angular.module('chaise.viewer')

    .value('vocabs', {});
})();
